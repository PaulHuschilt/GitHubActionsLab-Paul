# GitHub Actions Lab

## Workflow 1
This workflow demonstrates job dependencies using the needs keyword
Jobs execute sequentially with build, test,  deploy.

## Workflow 2
This workflow demonstrates parallel execution across multiple platforms.
Jobs run on Ubuntu, Windows, and macOS simultaneously.

## Key Concepts
- needs
- runs-on
- GitHub Actions workflows

## Challenges
Setting up the workflow directory and YAML syntax errors were resolved by validating YAML structure.
